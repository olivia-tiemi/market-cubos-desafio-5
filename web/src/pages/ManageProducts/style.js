import { Container } from '@mui/material';
import { styled } from '@mui/material/styles';

const StyledManageContainer = styled(Container)(() => ({
  width: '1920px',
  margin: '0 auto',
}));

export default StyledManageContainer;
